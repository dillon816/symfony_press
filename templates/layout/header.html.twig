<header class="site-header">
    <div class="header-row">
        <a class="brand" href="{{ path('home') }}">SymfonyPress</a>

        <nav class="nav">
            
            <a class="nav-link" href="{{ path('home') }}#articles">Articles</a>

            {% if app.user %}
                <a class="nav-link" href="{{ path('admin_article_index') }}">Back-office</a>
            {% endif %}
        </nav>

        <div class="account">
            {% if app.user %}
                <span class="account-email">{{ app.user.userIdentifier }}</span>
                <a class="btn btn-ghost" href="{{ path('app_logout') }}">Déconnexion</a>
            {% else %}
                <a class="btn btn-ghost" href="{{ path('app_login') }}">Connexion</a>
                <a class="btn btn-primary" href="{{ path('app_register') }}">Inscription</a>
            {% endif %}
        </div>
    </div>
</header>
